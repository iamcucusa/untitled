{
  "name": "monorepo-untitled",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -C apps/web-react dev",
    "lint": "echo 'add your linter setup'",
    "typecheck": "echo 'add ts typecheck jobs'"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ]
}